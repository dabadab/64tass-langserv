- F12 opening the wrong file - do the same check as for the undefined symbols, do not force finding something anyway
- run the assembler - if no root is specified use the same include tree as for the symbol definitions

